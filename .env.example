# Copy to .env and fill in values:
#   cp .env.example .env

ANTHROPIC_API_KEY=

# Optional: brief cache TTL in seconds (default 720 = 12 min).
# Events are refreshed from DataSF on cache miss only.
# SUMMARY_CACHE_TTL_SEC=720
